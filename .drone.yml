debug: false

pipeline:
  docker:
    image: plugins/docker
    environment:
      - DOCKER_LAUNCH_DEBUG=false
    repo: trenchleague/webapp
    registry: registry.stevem.io
    tags:
      - beta
      - latest
    dockerfile: Dockerfile
    insecure: false
    secrets: [ docker_username, docker_password ]
